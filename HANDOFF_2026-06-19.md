---
file: HANDOFF_2026-06-19.md
version: 1.0
author: Sam Cao (via Claude thread `inspiring-turing`)
created: 2026-06-19
last_updated: 2026-06-19
description: Thread-migration handoff. This thread shipped the live-playtest queue (v5.1.221–232), the full F1–F5 backlog, and the CUSTOM MAP EDITOR (v5.1.233–235). It ALSO locked the Tournament v2 design with Sam. Use this to pick up cleanly in a new thread. Read MIGRATION.md (⏩ CURRENT STATE) for terse facts; OVERNIGHT_PLAN.md for the backlog + the LOCKED tournament design; HANDOFF_2026-06-16.md for the deep combat/vision narrative.
ai_update: Living capture — mark items DONE inline as built, don't delete the reasoning. Append a changelog.
---

# DriveShowdown — handoff (2026-06-19, thread `inspiring-turing`)

> **START HERE for the next thread.** Tip is **v5.1.235 on `dev`, all green.** Three canonical docs:
> - **`MIGRATION.md`** → "⏩ CURRENT STATE" = terse facts (branches, build, what shipped).
> - **`OVERNIGHT_PLAN.md`** → the F1–F5 backlog + the **🏆 TOURNAMENT v2 — LOCKED DESIGN** block (the next big feature).
> - **`HANDOFF_2026-06-16.md`** → the deep narrative (combat vision, weapons, perks, eggs, cheats). Read for the *why*.

## 🚦 Fresh-container setup (ALWAYS, before tests)
The container is ephemeral and `/tmp/g.js` doesn't exist on a fresh clone:
```sh
./extract.sh && ./battery.sh    # prints "ALL GREEN" when healthy; exits non-zero on a real failure
```
A bare `ENOENT … /tmp/g.js` = missing setup, NOT a test failure.

## 🪢 Branch policy
Canonical branch = **`dev`**. Start: `git fetch origin dev`, base work on it. End: land commits on `dev` and `git push -u origin dev`. The session's auto `claude/*` branch is just a per-session default — reconcile back to `dev`. (This thread also pushed to `claude/inspiring-turing-a8yfss`.)

## 🛠️ How Sam works
Live-playtests on desktop + phone, brain-dumps fast (often 1 idea/message). Wants terse status, real `./battery.sh` results, one shippable green increment per fix, and a build file dropped at each version. Commit author `Claude <noreply@anthropic.com>` is fine. **Never push the model identifier into repo artifacts.** Build = single self-contained `drive_showdown_v5.1.NNN.html`; bump the filename + the `version:`/changelog header each ship, and update `extract.sh`'s filename.

---

## ✅ What this thread shipped (v5.1.221 → v5.1.235)
All green on `dev`. Full per-version detail in the HTML changelog header + MIGRATION.md. Highlights:
- **Live playtest queue:** push-ball physics + flavored weapon interactions + soccer ball/GOALS-TO-WIN (222/223/225/226), Original Sin livery + buffs (221/224/225), push-ball CPU weapon-awareness (227), 3v3 in-match sensitivity fix (228), controller compat on splash + 3v3 grid (229), absolute-heading↔drive-frame coupling (230).
- **F2 easter eggs:** egg achievements wired into the Achievements screen (231), custom PAINT picker — click a bot's 🎨 chassis preview to cycle a palette (232).
- **F5 CUSTOM MAP EDITOR — COMPLETE (233/234/235):** see below.

### Custom map editor — how it works (so you can extend it)
- **Entry:** RoboRumble → ARENA gallery → the green **"+ NEW MAP"** tile (`bbMapTiles` appends custom ids + `'new'`). Custom tiles show **✎ edit / 🗑 delete**.
- **Screen:** `phase==='p2mapedit'`, drawn by `drawMapEditor()`, state in `let mapEd={id,name,obs,haz,pup,tool,drag,undo,_touch}`.
- **Tools** (`MAP_TOOLS`): WALL + PIT = drag-to-draw rects; SAW + PICKUP = click to place; ERASE = click to remove topmost (`mapEditEraseAt`). Plus name field, CLEAR, UNDO (↶ / Z key, 40-deep `mapEd.undo` via `mapEdPush()`), **SAVE & USE**, and **📤 SHARE** (export).
- **Input:** driven by its OWN mousedown/move/up (`mapEditDown/Move/Up`) AND touchstart/move/end (tracks `mapEd._touch`); the `click` dispatcher early-returns for `p2mapedit`; mousedown sets `_bbDragSuppressClick` so a SAVE/BACK phase-change doesn't fall through.
- **Storage/resolve:** `customMaps` (localStorage key `frcds_maps_v1`), `saveCustomMaps()`, `customMapById()`, `bbMapObj()` resolves the ARENA setting → built-in index | `'rand'` (rolls over built-ins+custom) | custom id. RoboRumble match-start uses `bbMapObj()`; **Tank Fight stays built-in-only** (lines ~3305).
- **Share codes:** `mapToCode(m)` → `DSMAP1:`+base64(JSON); `mapFromCode(code)` validates + rejects garbage. 📥 IMPORT MAP button on the gallery (`mapImport()` via prompt). Workshop-ready JSON.
- **Tests:** `harness/tests/smoke57.js` (battlebots P1, now **515 asserts**) — editor create→draw→save→resolve→gallery→load, share round-trip, undo. `smoke52.js` gallery-count assert was updated for the NEW tile.

---

## 🏆 NEXT UP — Tournament v2 (F3). The big remaining feature.
**The full LOCKED design is in `OVERNIGHT_PLAN.md` → "🏆 TOURNAMENT v2 — LOCKED DESIGN (Sam, 2026-06-19)".** Read it. Summary of the decisions Sam made this thread:
- **Two formats (a setting):** KEEP the existing **single-elim** (party, ≤16) AND add **double-elim "FRC mode"** (≤8, losers' bracket + **best-of-3 grand final**).
- **Rosters = captain DRAFT** (FRC alliance-selection teaching moment): **8 human captains** build alliances of 3 by snake-drafting from a pool that **mixes remaining humans + pre-ranked CPUs** (turnout-flexible 8/12/16 humans). Model partly landed: `tourAllianceDraft`.
- **Seeding = a setting: MANUAL or RANDOM** (random to save time); CPUs fill empty slots either way. (`seedMode` already has rand + a qual option.)
- **Lineup timing = a setting:** `once` (at registration, FRC-standard — **build first**) or `perMatch` (configurable extension).
- **One mode for the whole bracket; the MAP varies per match.** Map-pick rule: **higher seed picks** during the bracket; **winners-bracket team picks at the grand final**; optional **open/group-pick with a countdown that auto-randomizes.**

### ▶️ Sam's chosen FIRST build (agreed this thread):
**The T4 "open pick with countdown" map picker — build it FIRST.** Rationale: it's the smallest standalone piece, has **no dependency** on the bracket/seeding infra (higher-seed gating needs that infra and comes later), it reuses the **map gallery this thread already generalized**, and it's useful even outside tournaments (a pre-match arena vote). Implementation sketch: show `drawBBMapPicker`-style gallery with a visible countdown; anyone can tap a tile; on timeout, auto-select a random arena; then start the match. Later layer the **higher-seed / winners-bracket** "whose turn to tap" gate on top of the same screen.

### Build phases (UI-heavy, hands-on — needs Sam in the loop):
① keep single-elim + add FORMAT setting (single ≤16 / double ≤8) → ② double-elim engine (losers' bracket + bo3 final) → ③ manual/random seeding + CPU tier auto-fill → ④ captain-draft screen (snake from the human+CPU pool → 3-bot rosters) → ⑤ T2 route bracket matches through the 6-seat grid with drafted lineups → ⑥ **T4 map-select (start with open-pick-with-timer)** → ⑦ T1-hard registration text-input fix.
> Note: ⑥'s open-pick-with-timer is the agreed entry point even though it's late in the dependency chain — it stands alone, so it can be built before ①–⑤.

## 🗂️ Other parked backlog (needs Sam, not started)
- **STOCK → 6-player FFA** (F4) — ~10h+, engine hard-wires 2 sides (~51 touchpoints). Playtest session, not unattended.
- **Career / Story mode** (future vision) — middle-school OBSTACLE RACE → high-school BALL modes → college TANK FIGHT → graduate ROBORUMBLE. Big new meta-shell. Captured in OVERNIGHT_PLAN.
- **Tabled tuning** (Sam): kamikaze 0%-in-3v3 CPU brain, tier lapse/react depth, jet CPU dash-through, repair heal↔buff cycle.

## ⚠️ Gotchas
- The 1v1 CPU-sim (`bbbalance.js`) is a **coarse directional tool, NOT a tuning target** (spinner 72 / pincer 11 / kamikaze 0 is fine).
- `battery.sh` exit code is trustworthy **only after** `extract.sh`.
- When you add a gallery tile type, update the count asserts in `smoke52.js` AND `smoke57.js`.

## Changelog
- 2026-06-19 (v1.0): created at v5.1.235. Map editor complete; tournament v2 design locked; T4 open-pick-with-timer chosen as the next first build.
